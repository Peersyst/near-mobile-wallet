import styled from "@peersyst/react-native-styled";
import { Row } from "react-native-components";

export const DaoBalanceRowRoot = styled(Row, { justifyContent: "space-between", alignItems: "center" })(() => ({
    height: 28,
}));

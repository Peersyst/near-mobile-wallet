import { Paper } from "@peersyst/react-native-components";
import styled from "@peersyst/react-native-styled";

export const MainHomeCard = styled(Paper)(() => ({
    flex: 1,
    borderBottomLeftRadius: 0,
    borderBottomRightRadius: 0,
}));

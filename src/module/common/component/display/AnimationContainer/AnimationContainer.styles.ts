import styled from "@peersyst/react-native-styled";
import { View } from "react-native";

export const AnimationContainerRoot = styled(View)(() => {
    return {
        alignItems: "center",
        justifyContent: "center",
        width: "100%",
        height: "100%",
    };
});

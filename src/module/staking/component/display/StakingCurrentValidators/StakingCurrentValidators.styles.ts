import styled from "@peersyst/react-native-styled";
import { Col } from "@peersyst/react-native-components";

export const StakingCurrentValidatorsRoot = styled(Col)(() => ({
    flex: 1,
    justifyContent: "center",
    paddingVertical: 16,
    paddingHorizontal: 20,
}));

import styled from "@peersyst/react-native-styled";
import { Col } from "@peersyst/react-native-components";

export const StakingDetailsRoot = styled(Col, { flex: 1 })(() => ({
    paddingVertical: 16,
    minHeight: 270,
    justifyContent: "center",
}));

import styled from "@peersyst/react-native-styled";
import { Paper } from "@peersyst/react-native-components";

export const MainStakingCard = styled(Paper)((theme) => ({
    flex: 1,
    backgroundColor: theme.theme.palette.background,
}));

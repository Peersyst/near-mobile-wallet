import { Col } from "@peersyst/react-native-components";
import styled from "@peersyst/react-native-styled";

export const ReceiveModalWrapper = styled(Col)(() => ({
    flex: 1,
    alignItems: "center",
}));

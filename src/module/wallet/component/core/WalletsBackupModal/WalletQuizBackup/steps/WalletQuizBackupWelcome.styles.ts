import { ElementStyler } from "@peersyst/react-native-components";
import styled from "@peersyst/react-native-styled";

export const WalletQuizBackupIcon = styled(ElementStyler)(({ theme }) => ({
    color: theme.palette.primary,
    fontSize: 90,
}));

import styled from "@peersyst/react-native-styled";
import SettingsMenuItem from "../../navigation/SettingsMenuItem/SettingsMenuItem";

export const WalletsBackupRoot = styled(SettingsMenuItem)(() => ({
    flexDirection: "column",
    gap: 10,
}));

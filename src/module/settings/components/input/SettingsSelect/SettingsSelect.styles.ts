import styled from "@peersyst/react-native-styled";
import SettingsCard from "../../display/SettingsCard/SettingsCard";

export const SettingsSelectRoot = styled(SettingsCard)(() => ({
    paddingHorizontal: 20,
    paddingBottom: 24,
    paddingTop: 16,
}));

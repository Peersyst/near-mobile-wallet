import { createTheme } from "@peersyst/react-native-components";

export const theme = createTheme({
    typography: {
        h1: {
            fontSize: 22,
        },
        h2: {
            fontSize: 18,
        },
        h3: {
            fontSize: 16,
        },
    },
    borderRadius: 24,
});
